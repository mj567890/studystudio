# StudyStudio 课程制作全流程 — 思维导图

> 以下 Mermaid mindmap 可在 VS Code（安装 Mermaid 插件）、GitHub、Obsidian 中直接渲染
> 也提供纯文本缩进版供导入 XMind / MindManager / 百度脑图

---

## Mermaid 思维导图（可直接渲染）

```mermaid
mindmap
  root((StudyStudio<br/>课程制作全流程))
    :::large
    前端入口
      UploadView.vue
        支持 PDF/DOCX/MD/TXT
        最大 100MB
        FormData 上传
      TutorialView.vue
        三种阅读模式
        速览/正常/深度
        章节测验
        AI 反思
        社交笔记
        证书下载
    阶段1_文件上传
      安全校验
        Content-Type 白名单
        文件名过滤（防路径穿越）
      SHA-256 去重
        同文件同空间 → 复用
        同文件不同空间 → 新记录
      MinIO 存储
        files/{id}/{filename}
        AsyncMinIOClient 异步封装
      事件发布
        EventBus → RabbitMQ
        file_uploaded 事件
        初始状态 uploaded
    阶段2_事件调度
      RabbitMQ 订阅
        file_uploaded → ingest 队列
        document_parsed → extraction 队列
      Celery 5 队列
        knowledge
        knowledge.review
        tutorial
        blueprint.synthesis
        low_priority
      超时策略
        ingest: 180s/240s
        extraction: 无限制
        review: 600s/720s
    阶段3_文档解析
      多格式引擎
        PDF → PyMuPDF(fitz)
        DOCX → python-docx
        MD/TXT → charset 检测
      文本分块
        RecursiveCharacterTextSplitter
        每页独立分块
        元数据标注
      截断保护
        MAX 500 块
        is_truncated 标记
      批量写入 document_chunks
      状态变为 parsed
    阶段4_知识提取
      原子锁
        UPDATE RETURNING
        防并发重复提取
      Step1 实体识别
        ENTITY_RECOGNITION_PROMPT
        提取教学实体名称
      Step2 实体分类
        ENTITY_CLASSIFICATION_PROMPT
        concept/element/flow/case/defense
        short_definition
      Step3 关系抽取
        RELATION_EXTRACTION_PROMPT
        prerequisite_of/related_to
        part_of/example_of
      去重策略
        跨块按 entity_name 合并
        跨文档按 canonical_name+domain
      状态变为 extracted
    阶段5_自动审核
      Round1 评审
        approve + conf≥0.75 → approved
        reject → rejected
        其余 → Round2
      Round2 评审
        approve + conf≥0.60 → approved
        其余 → rejected
      并发安全
        pg_try_advisory_lock
        空间级锁
      守护任务
        celery_beat 每5分钟
      状态变为 embedding
    阶段6_向量化
      嵌入文本构建
        canonical_name + short_definition
        截断512字符
      批量生成
        batch_size=32
        LLMGateway.embed()
      pgvector 存储
        PostgreSQL vector 类型
        索引加速
      Chunk 向量（并行）
        用于原文相似搜索
      状态变为 reviewed
    阶段7_蓝图生成
      V2 合成引擎
        Phase1 过滤加聚类
          过滤非教学实体
            CVE/版本号/厂商名
          K-Means 聚类
            K=max(4,round(N/7))
            上限30
          均衡化
            合并 <3 实体簇
            拆分 >12 实体簇
        Phase2 LLM 四步生成
          簇命名
            CLUSTER_CHAPTER_PROMPT
            标题+目标+任务+标准+误区
          阶段规划
            STAGE_PLANNING_PROMPT
            基础→实践→评估
          课程标题
            COURSE_TITLE_PROMPT
          章节正文
            CHAPTER_CONTENT_PROMPT
            600-900字
            结构化输出
        内容结构
          scene_hook（场景导入）
          skim_summary（速览要点）
          full_content（正文+CHECKPOINT）
          code_example（代码示例）
          misconception_block（误区）
          prereq_adaptive（自适应扩展）
      两种模式
        模式A：全新生成
          首次创建课程
          K-Means 从头聚类
        模式B：增量合并
          追加文档
          余弦相似度差异
          >0.92 跳过
          0.75-0.92 补充
          <0.75 新章节
      状态变为 published
    阶段8_测验生成
      关联实体读取
      QUIZ_GENERATION_PROMPT
      写入 chapter_quizzes
      学生端随机抽题
      ≥60% 自动已读
    阶段9_学生学习
      三种阅读模式
        速览 skim_summary
        正常 full_content
        深度 +prereq_adaptive
      互动功能
        章节测验
        AI 反思
        社交笔记
        源文档追溯
        关联推荐
      证书下载
    阶段10_教师精调
      Layer1 生成前约束
        上传时输入
        全局教学指令
        注入所有Prompt
      Layer2 对话式精调
        自然语言指令
        ElMessageBox textarea
        CHAPTER_REFINEMENT_PROMPT
        refinement_version+1
        require_space_owner 鉴权
      Layer3 联动再生
        测验缓存失效
        题目重新生成
        讨论种子更新
    技术栈
      前端
        Vue 3 + TS
        Vite
        Element Plus
        Pinia
      API
        FastAPI
        Pydantic v2
        Uvicorn
      AI
        LangChain
        LLMGateway
        PyMuPDF
      ���储
        PostgreSQL+pgvector
        MinIO/S3
        Redis
      任务
        Celery
        RabbitMQ
        EventBus
      规模
        27迁移
        144测试
        9服务
        10阶段
        5队列
        3迭代层
```

---

## 纯文本缩进版（导入 XMind / MindManager / 百度脑图）

```
StudyStudio 课程制作全流程
├── 一、前端入口
│   ├── UploadView.vue（文件上传）
│   │   ├── 支持格式：PDF / DOCX / MD / TXT
│   │   ├── 最大 100MB
│   │   └── FormData 封装 → fileApi.upload()
│   └── TutorialView.vue（课程学习）
│       ├── 三种阅读模式：速览 / 正常 / 深度
│       ├── 章节测验 → ≥60% 自动已读
│       ├── AI 反思 → LLM 评分反馈
│       ├── 社交笔记 → 点赞互动
│       └── 证书下载（全部已读后）
│
├── 二、10 阶段自动化流水线
│   ├── 阶段 1：文件上传与存储
│   │   ├── 安全校验
│   │   │   ├── Content-Type 白名单检查
│   │   │   └── 文件名安全过滤（防路径穿越）
│   │   ├── SHA-256 去重
│   │   │   ├── 同文件同空间 → 复用已有记录
│   │   │   └── 同文件不同空间 → 新建文档记录
│   │   ├── MinIO 对象存储
│   │   │   ├── 键格式：files/{file_id}/{safe_filename}
│   │   │   └── AsyncMinIOClient 异步封装
│   │   └── 事件发布
│   │       ├── EventBus → RabbitMQ
│   │       ├── "file_uploaded" 事件
│   │       └── 文档状态 → uploaded
│   │
│   ├── 阶段 2：事件驱动任务调度
│   │   ├── RabbitMQ 订阅注册
│   │   │   ├── file_uploaded → knowledge.ingest.queue
│   │   │   └── document_parsed → knowledge.extraction.queue
│   │   ├── Celery 5 个专用队列
│   │   │   ├── knowledge（解析 + 提取 + 向量）
│   │   │   ├── knowledge.review（自动审核）
│   │   │   ├── tutorial（教程相关）
│   │   │   ├── blueprint.synthesis（蓝图合成）
│   │   │   └── low_priority（低优先级）
│   │   └── 超时策略
│   │       ├── ingest：180s 软 / 240s 硬
│   │       ├── extraction：无限制
│   │       └── review：600s 软 / 720s 硬
│   │
│   ├── 阶段 3：文档解析与文本分块
│   │   ├── 多格式解析引擎
│   │   │   ├── PDF → PyMuPDF (fitz) 按物理页 + TOC
│   │   │   ├── DOCX → python-docx 段落级
│   │   │   └── MD/TXT → charset 检测解码
│   │   ├── 文本分块
│   │   │   ├── RecursiveCharacterTextSplitter (LangChain)
│   │   │   ├── 每页独立分块（保留页面边界）
│   │   │   └── 元数据：index_no / title_path / page_no / token_count
│   │   ├── 截断保护
│   │   │   ├── MAX_CHUNK_COUNT = 500
│   │   │   └── 超出标记 is_truncated
│   │   └── 完成动作
│   │       ├── 批量 INSERT document_chunks（50/批）
│   │       ├── 状态 → parsed
│   │       └── 发布 document_parsed + 触发 chunk embedding
│   │
│   ├── 阶段 4：知识提取（三步 LLM 流水线）
│   │   ├── 原子锁
│   │   │   └── UPDATE RETURNING 防并发重复提取
│   │   ├── Step 1 — 实体识别
│   │   │   ├── Prompt：ENTITY_RECOGNITION_PROMPT
│   │   │   └── 输出：实体名称列表
│   │   ├── Step 2 — 实体分类
│   │   │   ├── Prompt：ENTITY_CLASSIFICATION_PROMPT
│   │   │   ├── 类别：concept / element / flow / case / defense
│   │   │   └── 附加 short_definition
│   │   ├── Step 3 — 关系抽取
│   │   │   ├── Prompt：RELATION_EXTRACTION_PROMPT
│   │   │   ├── 关系：prerequisite_of / related_to / part_of / example_of
│   │   │   └── 仅对 2+ 新实体的 chunk 执行
│   │   ├── 去重策略
│   │   │   ├── 跨块：按 entity_name 合并
│   │   │   └── 跨文档：同 canonical_name + 同 domain_tag 跳过
│   │   └── 完成动作
│   │       ├── 批量写入 knowledge_entities + knowledge_relations
│   │       ├── 状态 → extracted
│   │       └── 派发 auto_review_entities（countdown=5s）
│   │
│   ├── 阶段 5：AI 自动审核（两轮质量把关）
│   │   ├── Round 1
│   │   │   ├── LLM 逐实体评审
│   │   │   ├── approve + confidence ≥ 0.75 → approved
│   │   │   ├── reject → rejected
│   │   │   └── 其余 → Round 2
│   │   ├── Round 2
│   │   │   ├── 更严格审核
│   │   │   ├── approve + confidence ≥ 0.60 → approved
│   │   │   └── 其余 → rejected
│   │   ├── 并发安全
│   │   │   └── pg_try_advisory_lock() 空间级锁
│   │   ├── 守护任务
│   │   │   └── resume_pending_review 每 5 分钟 celery_beat
│   │   └── 完成动作
│   │       ├── 文档状态 → embedding
│   │       └── 触发 backfill_entity_embeddings
│   │
│   ├── 阶段 6：向量化生成
│   │   ├── 嵌入文本构建
│   │   │   ├── canonical_name + " — " + short_definition
│   │   │   └── 截断 512 字符
│   │   ├── 批量生成
│   │   │   ├── batch_size = 32
│   │   │   └── LLMGateway.embed() 调用
│   │   ├── pgvector 存储
│   │   │   ├── PostgreSQL vector 类型
│   │   │   └── 索引加速检索
│   │   ├── Chunk 向量并行
│   │   │   └── embed_document_chunks 原文相似搜索
│   │   └── 完成动作
│   │       ├── _trigger_blueprint_if_ready() 检查
│   │       ├── 状态 → reviewed
│   │       └── 派发 synthesize_blueprint
│   │
│   ├── 阶段 7：蓝图/课程生成（V2 合成引擎）
│   │   ├── Phase 1：过滤 + 聚类
│   │   │   ├── 过滤非教学实体（CVE / 版本号 / 厂商名）
│   │   │   ├── K-Means 聚类
│   │   │   │   ├── K = max(4, round(N/7))
│   │   │   │   └── 上限 30
│   │   │   └── 均衡化
│   │   │       ├── 合并 < 3 实体的簇
│   │   │       └── 拆分 > 12 实体的簇
│   │   ├── Phase 2：LLM 四步生成
│   │   │   ├── ① 簇命名 → CLUSTER_CHAPTER_PROMPT
│   │   │   │   └── 标题 + 目标 + 任务描述 + 通过标准 + 常见误区
│   │   │   ├── ② 阶段规划 → STAGE_PLANNING_PROMPT
│   │   │   │   └── 基础 → 实践 → 评估（动词多样化）
│   │   │   ├── ③ 课程标题 → COURSE_TITLE_PROMPT
│   │   │   └── ④ 章节正文 → CHAPTER_CONTENT_PROMPT
│   │   │       └── 600-900 字结构化内容
│   │   ├── 章节内容结构
│   │   │   ├── scene_hook（场景导入）
│   │   │   ├── skim_summary（速览要点列表）
│   │   │   ├── full_content（正文 + CHECKPOINT 断点）
│   │   │   ├── code_example（代码示例）
│   │   │   ├── misconception_block（常见误区）
│   │   │   └── prereq_adaptive（自适应扩展）
│   │   ├── 两种工作模式
│   │   │   ├── 模式 A：全新生成
│   │   │   │   └── 首次创建课程，K-Means 从头聚类
│   │   │   └── 模式 B：增量合并
│   │   │       ├── 余弦相似度差异分析
│   │   │       ├── > 0.92：已覆盖，跳过
│   │   │       ├── 0.75-0.92：补充到最相似章节
│   │   │       ├── < 0.75：生成新章节
│   │   │       └── 进度自动重映射
│   │   └── 完成动作
│   │       ├── 状态 → published
│   │       └── 派发 pregen_chapter_quizzes
│   │
│   ├── 阶段 8：测验预生成
│   │   ├── 读取每章 core_term 实体
│   │   ├── 调用 QUIZ_GENERATION_PROMPT
│   │   ├── 写入 chapter_quizzes 表
│   │   └── 学生端随机抽题（≥60% 自动已读）
│   │
│   ├── 阶段 9：学生学习与交互
│   │   ├── 数据加载
│   │   │   ├── 选择领域 → 加载课程
│   │   │   └── 加载学习进度
│   │   ├── 三种阅读模式
│   │   │   ├── 速览：skim_summary 要点
│   │   │   ├── 正常：完整正文 + CHECKPOINT
│   │   │   └── 深度：正文 + prereq_adaptive
│   │   └── 互动功能
│   │       ├── 章节测验（随机抽题）
│   │       ├── AI 反思（LLM 评判理解总结）
│   │       ├── 社交笔记（点赞互动）
│   │       ├── 源文档追溯（查看原始片段）
│   │       ├── 关联推荐（跨课程推荐）
│   │       └── 证书下载（全部已读后）
│   │
│   └── 阶段 10：教师引导式迭代（三层方案）
│       ├── Layer 1：生成前约束
│       │   ├── 上传时输入全局教学指令
│       │   ├── 注入所有 LLM Prompt
│       │   └── 控制风格 / 难度 / 受众
│       ├── Layer 2：对话式章节精调
│       │   ├── 入口："✨ 精调本章" 按钮
│       │   ├── 交互：ElMessageBox.prompt textarea
│       │   ├── API：POST /admin/courses/chapters/{id}/refine
│       │   ├── 鉴权：require_space_owner()（仅课程所有者）
│       │   └── 版本：refinement_version + 1
│       └── Layer 3：附属内容联动
│           ├── 测验缓存自动失效
│           ├── 题目重新生成
│           └── 讨论种子可选更新
│
├── 三、文档状态机
│   ├── uploaded（已上传）
│   ├── parsed（已解析）
│   ├── extracting（提取中 🔒 原子锁）
│   ├── extracted（已提取）
│   ├── embedding（向量化中）
│   ├── reviewed（已审核）
│   └── published（已发布）
│
├── 四、事件链路（RabbitMQ）
│   ├── file_uploaded → knowledge.ingest.queue
│   ├── document_parsed → knowledge.extraction.queue
│   ├── entities_reviewed → (内部调用)
│   ├── embeddings_complete → (内部调用)
│   └── blueprint_published → blueprint.notify
│
├── 五、技术栈
│   ├── 前端
│   │   ├── Vue 3 + TypeScript
│   │   ├── Vite 打包
│   │   ├── Element Plus UI 库
│   │   └── Pinia 状态管理
│   ├── API 网关
│   │   ├── FastAPI 异步框架
│   │   ├── Pydantic v2 数据校验
│   │   ├── Uvicorn 服务
│   │   └── WebSocket 支持
│   ├── AI / 文档处理
│   │   ├── LangChain 文本处理
│   │   ├── LLMGateway 多模型网关
│   │   ├── PyMuPDF (fitz) PDF 解析
│   │   └── python-docx Word 解析
│   ├── 存储层
│   │   ├── PostgreSQL + pgvector（结构化 + 向量）
│   │   ├── MinIO / S3（对象存储）
│   │   └── Redis（结果缓存）
│   └── 任务系统
│       ├── Celery 异步任务
│       ├── RabbitMQ 消息队列
│       └── EventBus 内部事件总线
│
└── 六、项目规模指标
    ├── 27 个数据库迁移
    ├── 144 个测试用例（0 失败）
    ├── 9 个 Docker 服务
    ├── 10 个自动化阶段
    ├── 5 个 Celery 专用队列
    └── 3 层教师迭代方案
```
