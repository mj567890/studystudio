-- 029_system_default_template.sql
-- 新增“系统默认”完整课程结构模板，作为自定义模板的起点
-- 同时重命名旧的“标准教程”为“快速上手”

BEGIN;

-- 重命名旧模板
UPDATE course_templates SET name = '快速上手' WHERE name = '标准教程';

-- 插入综合系统默认模板
INSERT INTO course_templates (name, content, is_system, is_public) VALUES (
  '系统默认',
  '【课程整体结构】
课程必须分为三个阶段：基础认知（foundation）→ 工具实践（practice）→ 综合应用（assessment）。每阶段3-6章，难度逐步递进。

【每章结构要求 — 必须严格遵守】
1. scene_hook（场景导入）：100字以内，以"你"开头描述真实职场情境，引出本章核心问题
2. skim_summary（速览要点）：3条要点，分号分隔，每条不超过20字
3. full_content（正文）：600-900字，结构为"概念解释 → 原理/机制 → 操作步骤 → 实际示例"。术语首次出现用【术语名】标注。在关键转换点插入1-2个 <!--CHECKPOINT:问题|解析提示--> 标记，引导学生暂停思考
4. code_example（代码示例）：提供1个完整可运行示例，用三个反引号围栏包裹并标注语言类型。不涉及编程的章节填空字符串
5. misconception_block（误区纠正）：以"⚠ 很多人误认为…实际上…"的格式，针对本章最常见的1个误解进行纠正
6. prereq_adaptive（自适应扩展）：针对基础较好的学员，补充更深层技术细节、边界案例或进阶应用场景，100字以内

【代码格式规则】
- 代码块必须使用三个反引号围栏（```python / ```sql / ```bash 等），围栏内写完整代码
- 行内代码使用单个反引号包裹，如 `nginx -t`
- 禁止在代码围栏内写伪代码或纯中文描述

【教学风格】
语言正式但不生硬，理论讲解与实操并重。避免纯理论堆砌，每引入一个概念都要跟随一个"为什么"的解释。面向需要实际工作中使用的职业学习者。',

  TRUE, TRUE
);

-- 更新现有模板的顺序标记
-- 系统默认排在第一位，其他按原序
COMMIT;
